# bitwise-34878

- upstream id: 34878
- source type: pr
- mechanism: deterministic_dispatch_reduction
- current status: stable
- decision: include
- priority: medium
- last reviewed: 2026-06-21

## Why not promoted

这是 ROCm test-harness boundary，不等于生产默认关闭所有 batch-size-sensitive ROCm kernel，也不证明所有 ROCm serving path 已 bitwise。

## Evidence summary

新增 insight：beam search/ranking 对 `1e-5` 级 logprob drift 敏感；ROCm 测试必须固定 batch composition、CUDA graph padding、prefix side effect 和 GEMM path，避免 backend reduction drift 污染 beam-search 逻辑判断。

Target evidence: PR #34878 merged files: tests/samplers/test_beam_search.py, Buildkite sampler test config; ROCm engine kwargs async_scheduling=False/enforce_eager=True/enable_prefix_caching=False/max_num_seqs=1; VLLM_ROCM_USE_SKINNY_GEMM=0; review comment kept explicit per-test env setup

## Missing evidence

无阻塞性缺口；只保留适用边界和后续回归观察。

## Next action

已吸收到 verification_contracts 的 verification matrix；后续只在 ROCm production/selector 边界发生变化时再升回 queue。
