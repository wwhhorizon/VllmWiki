# vllm-project/vllm#33950: [Bug]:

| 字段 | 值 |
| --- | --- |
| Issue | [#33950](https://github.com/vllm-project/vllm/issues/33950) |
| 状态 | open |
| 标签 | bug;stale |
| 评论 | 1; 本地原始数据只有评论数量，没有评论正文 |
| 一级分类 | correctness |
| 工作域 | ci_build;distributed_parallel;frontend_api;gemm_linear;hardware_porting;model_support;sampling_logits;speculative_decoding |
| 子分类 | precision |
| Operator 关键词 | cuda;operator;sampling;triton |
| 症状 | build_error;nan_inf |
| 根因提示 | env_dependency |
| 硬件范围 | amd;nvidia |
| 需要人工复核 | False |

## 源证据

### Issue 标题

> [Bug]:

### Issue 正文摘录

### Your current environment ### 🐛 Describe the bug Openai sdks `reasoning_effort="none"` curretly not working ```python chat_completion = client.chat.completions.create( messages=[ {"role": "user", "content": prompt}, ], reasoning_effort="none", model=model, ) ``` vllm server output ``` (APIServer pid=48490) INFO: 127.0.0.1:54024 - "POST /v1/chat/completions HTTP/1.1" 400 Bad Request ``` I've started vllm using ```bash vllm serve Qwen/Qwen3-4B \ --max-model-len 8192 \ --gpu-memory-utilization 0.85 \ --port 8000 \ --enforce-eager ``` ```python from openai import OpenAI openai_api_key = "EMPTY" openai_api_base = "http://localhost:8000/v1" client = OpenAI( # defaults to os.environ.get("OPENAI_API_KEY") api_key=openai_api_key, base_url=openai_api_base, ) models = client.models.list() print(f"Available models: {",".join([d.id for d in models.data])}") model = models.data[0].id chat_completion = client.chat.completions.create( messages=[ {"role": "user", "content": "Hi"}, ], reasoning_effort="none", model=model, ) print("Chat completion results:") print(chat_completion) ``` and my output from the .py code ``` openai.BadRequestError: Error code: 400 - {'error': {'message': '1 validation...

## 候选优化模式

- [构建、依赖与打包](../patterns/build_dependency_packaging.md) - 分数 4: tion 0.85 \ --port 8000 \ --enforce-eager ``` ```python from openai import OpenAI openai_api_key = "EMPTY" openai_api_base = "http://localhost:8000/v1" client = OpenAI( # defaults to os.environ.get("OPENAI_API_KEY") api...
- [Scheduler 与请求状态生命周期](../patterns/scheduler_request_lifecycle.md) - 分数 3: [Bug]: bug;stale ### Your current environment ### 🐛 Describe the bug Openai sdks `reasoning_effort="none"` curretly not working ```python chat_completion = client.chat.completions.create( messages=[ {"role": "user
- [硬件架构 Guard](../patterns/hardware_arch_guard.md) - 分数 2: ot! ### Before submitting a new issue... - [x] Make sure you already searched for relevant issues, and asked the chatbot living at the bottom right corner of the [documentation page](https://docs.vllm.ai/en/latest/), wh...
- [模型格式与 Adapter 路径](../patterns/model_format_adapter.md) - 分数 2: le": "user", "content": prompt}, ], reasoning_effort="none", model=model, ) ``` vllm server output ``` (APIServer pid=48490) INFO: 127.0.0.1:54024 - "POST /v1/chat/completions HTTP/1.1" 400 Bad Request ``` I've started...
- [Backend 路由与 Fallback](../patterns/backend_routing_fallback.md) - 分数 1: odel_support;sampling_logits;speculative_decoding cuda;operator;sampling;triton build_error;nan_inf env_dependency Your current environment

## Wiki 抽取状态

- 风险：该 issue 有评论，但本地数据只有评论数量，没有评论正文。
- 本地没有 linked-fix 证据；目前只支持症状/路径抽取。
- 后续迭代应在可用时读取完整讨论评论。
