# bitwise-41511
- upstream id: 41511
- source type: issue
- mechanism: quant_dtype_semantics
- current status: defer_blocked
- decision: include
- priority: high
- last reviewed: 2026-06-23

## Why defer_blocked
W4A16 MoE weight_scale not sharded along K under tensor parallelism → kernel computes wrong group_size (1x8 mismatch = K-shard ratio). Directly quant_dtype_semantics + TP sharding correctness. PR #38222 (mentioned) is a related but different root cause (Marlin shape alignment for non-128 hidden dims). No direct fix for the TP scale sharding bug. Issue open, 1 comment.

## Evidence summary
Issue open. Detailed bisect: config reports group_size=128 but kernel-dispatch sees group_size=16 under TP=8. PRs #38222 (dimension alignment check, mentioned), #40991 (SM12x DSv4 support, mentioned), #41276 (DSV4 quant support, WIP) — none directly fix the TP scale sharding.

## Next action
Track if any PR addresses weight_scale K-sharding under TP for compressed-tensors W4A16 MoE; verify whether #41276 lands a fix.
