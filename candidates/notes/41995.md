# bitwise-41995

- upstream id: 41995
- source type: pr
- mechanism: kv_cache_identity
- current status: defer_blocked
- decision: defer
- priority: medium
- last reviewed: 2026-06-22

## Why not promoted

Open PR (6 comments, 0 review comments, 2 changed files); Mamba copy metadata device pointer high-bit truncation: XPU pointers >= 2**63 stored as signed int get truncated. Fix: store as uint64 bit pattern. Test: test_collect_mamba_copy_meta_accepts_high_bit_device_pointers with src_ptr=2**63+17, dst_ptr=2**63+41.

## Evidence summary

新增索引：KV cache metadata identity 的 pointer 位宽维度——XPU pointers >= 2**63 用有符号整数存储会被截断。与 #34874 metadata buffer pointer 同一机制家族但关注点位宽。PR 仍 open。

Target evidence: PR #41995 snippet: test_collect_mamba_copy_meta_accepts_high_bit_device_pointers; XPU pointers may be >= 2**63; store by uint64 bit pattern; _FakeState([0, dst_ptr], element_size=2)

## Missing evidence

Open PR (6 comments, 0 review comments, 2 changed files); Mamba copy metadata device pointer high-bit truncation: XPU pointers >= 2**63 stored as signed int get truncated. Fix: store as uint64 bit pattern. Test: test_collect_mamba_copy_meta_accepts_high_bit_device_pointers with src_ptr=2**63+17, dst_ptr=2**63+41.

## Next action

追踪 #41995 是否合并；若合并确认 pointer 存储是否统一用 uint64 bit pattern。
