# bitwise-42513

- upstream id: 42513
- source type: issue
- mechanism: batch_invariance
- current status: defer_blocked
- decision: defer
- priority: low
- last reviewed: 2026-06-20

## Why not promoted

`#42513` 只是最小入口；真正收口在 `#42518`。maintainer 明确评论这类 eager drift 是 known issue，若需要 exact invariance 应使用 `VLLM_BATCH_INVARIANT=1`。因此当前缺口不再是“关闭原因未知”，而是没有官方 selective fix、changed files 或 regression test 去重新定义默认契约。

## Evidence summary

新增 insight：MTP/spec decode 也可能通过 verification batch geometry 改变 eager attention GEMM 数值路径，并经 KV 放大到 token 分叉；但当前上游把它收口成 batch-invariant 契约边界，而不是独立 landed fix。

Target evidence: Issue #42513 body/timeline; issue #42518 body, comments, maintainer closure: known issue, use batch-invariant mode for exact reproducibility

## Missing evidence

`#42513` 只是最小入口；真正收口在 `#42518`。maintainer 明确评论这类 eager drift 是 known issue，若需要 exact invariance 应使用 `VLLM_BATCH_INVARIANT=1`。因此当前缺口不再是“关闭原因未知”，而是没有官方 selective fix、changed files 或 regression test 去重新定义默认契约。

## Next action

按辅助边界维护；只有出现 selective fix、官方 eager-vs-BI contract test、文档化声明变化或新的 changed files，再升回主线 direct-closure 复核。
