# bitwise-42670

- upstream id: 42670
- source type: pr
- mechanism: batch_invariance
- current status: include_with_boundary
- decision: include
- priority: high
- last reviewed: 2026-06-21

## Why not promoted

PR 仍 open/unmerged；模型级复现需要 MiniMax-M2 NVFP4 + B200/TP=2/concurrency=2，不适合 CI；scope 是 FlashInfer/CUTLASS FP4 MoE support gate，不是修复 checkpoint 对低精度噪声敏感的根因。

## Evidence summary

新增 insight：batch-invariant path 需要 backend support gate/selector/oracle 同步暴露；已有 deterministic code path 被 base-class False 拦住时，用户 env var 等于不可达。本轮（2026-06-21）复核 GitHub：PR 仍 open，最近提交/评论停留在 2026-05-14，多位 reviewer 仍 awaiting requested review，无 maintainer acceptance/merge 信号，标签维持 support-gate workaround 不变。

Target evidence: PR #42670 files: flashinfer.py supports_batch_invariance; cutlass_moe.py _supports_batch_invariance; PR body test plan sha256 identical under VLLM_BATCH_INVARIANT=1

## Missing evidence

PR 仍 open/unmerged；模型级复现需要 MiniMax-M2 NVFP4 + B200/TP=2/concurrency=2，不适合 CI；scope 是 FlashInfer/CUTLASS FP4 MoE support gate，不是修复 checkpoint 对低精度噪声敏感的根因。

## Next action

追踪 PR merge/CI；补轻量 selector/support-gate test；不要外推到非 MiniMax-M2 NVFP4 或非 FlashInfer/CUTLASS FP4 MoE。
