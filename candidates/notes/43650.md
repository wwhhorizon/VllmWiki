# bitwise-43650

- upstream id: 43650
- source type: pr
- mechanism: prefix_cache_equivalence
- current status: defer_blocked
- decision: defer
- priority: medium
- last reviewed: 2026-06-22

## Why not promoted

Open PR (7 comments, 0 review comments, 1 changed file); MTP/eagle cache hit search boundary differs between Full-attention and Mamba/GDN: Full-attention matches extra block then pops, but Mamba/GDN state blocks pop removes state itself. Fix: reduce search boundary by 1 for eagle/MTP.

## Evidence summary

新增索引：prefix cache hit search 边界在 Full-attention 与 Mamba/GDN 间的结构差异——Full-attention 可多匹配后 pop，Mamba/GDN 的 pop 会移除 state。与 #34865/#34874 同一机制家族但关注点不同。PR 仍 open。

Target evidence: PR #43650 snippet: max_num_blocks -= 1 for eagle/MTP; Full-attention cache hit lookup matches one extra block then drops; Mamba/GDN state blocks are [null,...,state] so popping removes state

## Missing evidence

Open PR (7 comments, 0 review comments, 1 changed file); MTP/eagle cache hit search boundary differs between Full-attention and Mamba/GDN: Full-attention matches extra block then pops, but Mamba/GDN state blocks pop removes state itself. Fix: reduce search boundary by 1 for eagle/MTP.

## Next action

追踪 #43650 是否合并；若合并可作为 prefix-cache hit search 在 hybrid 模型上的 landed boundary。
