# bitwise-38887
- upstream id: 38887
- source type: issue_pr_pair
- mechanism: verification_contract
- current status: include_with_boundary
- decision: include
- priority: medium
- last reviewed: 2026-06-23

## Why include_with_boundary
Gemma4 heterogeneous head dims (head_dim=256, global_head_dim=512) force TRITON_ATTN fallback "to prevent mixed-backend numerical divergence." This is directly a verification_contract insight: vLLM deliberately restricts backend to avoid mixed-backend numerical drift. PR #38891 (closes_keyword) fixes by allowing per-layer attention backend selection. Boundary: the fix is primarily about performance (allowing FlashAttention on compatible layers), not a determinism closure — the numerical divergence prevention is the motivation, not the fix target.

## Evidence summary
Issue open, 8 comments. PR #38891 (closes_keyword, confidence 0.95) "Allow per-layer attention backend selection for heterogeneous head dims." Log line confirms vLLM forces TRITON_ATTN to prevent mixed-backend numerical divergence.

## Next action
Track if #38891 merges; verify whether per-layer backend selection preserves or weakens the numerical divergence guard.
