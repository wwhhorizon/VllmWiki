# bitwise-7160
- upstream id: 7160
- source type: issue_pr_pair
- mechanism: verification_contract
- current status: include_with_boundary
- decision: include
- priority: medium
- last reviewed: 2026-06-23

## Why not promoted
Closed issue with linked bugfix PR #7164. The local evidence strongly supports a landed input-processor bug, but for now I am keeping it as an include-with-boundary item because the current corpus only exposes the summary-level closure.

## Evidence summary
The issue is a hard mismatch between expected image-token count and produced image embedding size for InternVL2. The linked PR is explicitly an input-processor fix. This is useful for the bitwise line as a verification-contract case: multimodal correctness depends on exact token / embedding-count agreement, not just semantically similar outputs.

