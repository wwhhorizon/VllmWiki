# bitwise-42821
- upstream id: 42821
- source type: issue_pr_pair
- mechanism: deterministic_dispatch_reduction
- current status: defer_blocked
- decision: include
- priority: high
- last reviewed: 2026-06-23

## Why defer_blocked
model.load_weights second call silently corrupts MoE forward into "multilingual subword soup" — symptom tagged nondeterministic. Root cause: PR #36286 moved per-backend kernel-layout transforms (swap_w13_to_w31 for FlashInfer CUTLASS/TRTLLM) into _setup_kernel, making load_weights non-idempotent. PR #42823 (closes_keyword, confidence 0.95) fixes by making load_weights safe on already-initialized model. Issue open, 0 comments — fix not yet confirmed landed/merged.

## Evidence summary
Issue open. PR #42823 (closes_keyword) "Make model.load_weights safe to invoke on already-initialized model." Regression bisected to PR #36286 (MoE Refactor, Migrate Unquantized to Full Oracle Flow). Affects FLASHINFER_CUTLASS and FLASHINFER_TRTLLM backends.

## Next action
Verify if PR #42823 is merged; if confirmed landed with test coverage, promote to stable.
