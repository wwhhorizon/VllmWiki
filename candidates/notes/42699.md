# bitwise-42699

- upstream id: 42699
- source type: issue
- mechanism: prefix_cache_equivalence
- current status: defer_blocked
- decision: defer
- priority: low
- last reviewed: 2026-06-20

## Why not promoted

open issue；评论复现 prefix-read/no-prefix-read greedy token split；`fp32` 或 `VLLM_BATCH_INVARIANT=1` 可让输出收敛，且评论把该缓解直接指向已合并的 #40193。当前缺的已不再是泛化的“原因未知”，而是 prefix-cache 专属 linked fix、官方 docs 定位或 regression test 去重新定义默认 exact-reproducibility 契约。

## Evidence summary

新增 insight：这条线当前更像默认 prefix-cache path 下的 exact reproducibility 契约边界；若 `fp32` 与 `VLLM_BATCH_INVARIANT=1` 都能消除复现，更支持 batch/kernel geometry 数值路径而非独立 KV corruption。

Target evidence: Issue #42699 body/comments: Qwen2-0.5B BF16 prefix-read vs full-recompute lexical split; comments show fp32/BI convergence and reference merged PR #40193

## Missing evidence

见上方 Why not promoted；核心缺口已在该段说明。

## Next action

按辅助边界维护；优先追踪 docs 声明、prefix-cache 专属 regression test 或 linked patch，除非上游重新把问题收口成独立 prefix-cache bugfix。
