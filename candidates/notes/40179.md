# bitwise-40179

- upstream id: 40179
- source type: pr
- mechanism: prefix_cache_equivalence
- current status: include_with_boundary
- decision: include
- priority: high
- last reviewed: 2026-06-19

## Why not promoted

PR 仍 open/dirty 且有 merge conflict；review comment 直接命中当前 patch：request.num_output_tokens > 0 会漏掉 resumed request，remainder == 0 会漏掉 block-aligned prompt 与 final-token scheduler 约束。

## Evidence summary

PR body 和 changed files 明确实现 deterministic prefix caching，并解释 full-prefill 与 suffix-prefill GEMM geometry 差异；本轮新增 insight：split boundary 应基于 num_computed_tokens 与 (num_prompt_tokens - 1) 的 final-token-aware block boundary，而不是 output_tokens/remainder 早退。

Target evidence: scheduler.py _prefix_cache_aligned_split; tests/v1/e2e/test_deterministic_prefix_caching.py; review comment on resumed/block-aligned prompt

## Missing evidence

PR 仍 open/dirty 且有 merge conflict；review comment 直接命中当前 patch：request.num_output_tokens > 0 会漏掉 resumed request，remainder == 0 会漏掉 block-aligned prompt 与 final-token scheduler 约束。

## Next action

追踪 follow-up patch 或 maintainer resolution；需要 resumed request、block-aligned prompt 和 final-token-aware boundary 回归测试后才可降级 risk。
