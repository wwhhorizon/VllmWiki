# vllm-project/vllm#1827: The default GPU number used is cuda:0, how do I specify the gpu number to use? For example cuda:2, I didn't find anything to change

| 字段 | 值 |
| --- | --- |
| Issue | [#1827](https://github.com/vllm-project/vllm/issues/1827) |
| 状态 | closed |
| 标签 |  |
| 评论 | 2; 本地原始数据只有评论数量，没有评论正文 |

## 源证据

### Issue 标题

> The default GPU number used is cuda:0, how do I specify the gpu number to use? For example cuda:2, I didn't find anything to change

### Issue 正文摘录

_本地原始数据中没有 issue 正文。_

## 候选优化模式

- [构建、依赖与打包](../patterns/build_dependency_packaging.md) - 分数 1: The default GPU number used is cuda:0, how do I specify the gpu number to use? For example cuda:2, I didn't find anything to change
- [硬件架构 Guard](../patterns/hardware_arch_guard.md) - 分数 1: The default GPU number used is cuda:0, how do I specify the gpu number to use? For example cuda:2, I didn't find anything to change

## Wiki 抽取状态

- 风险：该 issue 有评论，但本地数据只有评论数量，没有评论正文。
- 该 issue 不在当前 operator/kernel case 表中；保留索引，但暂不推断优化结论。
- 后续迭代应在可用时读取完整讨论评论。
