# bitwise-27660

- upstream id: 27660
- source type: pr
- mechanism: deterministic_dispatch_reduction
- current status: stable
- decision: include
- priority: high
- last reviewed: 2026-06-20

## Why not promoted

已 merged；核心机制是为 `torch.compile` 路径补 cuBLAS/reduced-precision/workspace 的 batch-invariant 控制，并显式关闭未闭环的 AOT compile 组合。

## Evidence summary

新增 insight：compile path 的 determinism 不是 compile/eager 二选一；要管住 cuBLAS reduced-precision/split-K/workspace algorithm，并把 Dynamo trace side effect 与 AOT compile 分开。AOT compile 的未覆盖边界不否定这条 merged 核心机制本身。

Target evidence: PR #27660 files: model.py removes BI enforce_eager; batch_invariant.py sets CUDA matmul reduced-precision flags, cublaslt preference, CUBLAS_WORKSPACE_CONFIG/CUBLASLT_WORKSPACE_SIZE fallback; envs.py disables VLLM_USE_AOT_COMPILE under BI; fp8.py precomputes DeepGEMM support for Dynamo; reviews discuss PyTorch 2.10 and AOT boundary

## Missing evidence

无阻塞性缺口；只保留适用边界和后续回归观察。

## Next action

主机制已稳定；若后续 AOT compile 重新开启或 PyTorch flag 行为改变，再单独补多 batch/M 维 logprob equality。
