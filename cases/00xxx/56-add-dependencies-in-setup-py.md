# vllm-project/vllm#56: Add dependencies in setup.py

| 字段 | 值 |
| --- | --- |
| Issue | [#56](https://github.com/vllm-project/vllm/issues/56) |
| 状态 | closed |
| 标签 |  |
| 评论 | 0; 本地原始数据只有评论数量，没有评论正文 |

## 源证据

### Issue 标题

> Add dependencies in setup.py

### Issue 正文摘录

Add the complete list of dependencies to `setup.py` so that the package can be pip-installed at once.

## 候选优化模式

- [构建、依赖与打包](../patterns/build_dependency_packaging.md) - 分数 2: Add dependencies in setup.py Add the complete list of dependencies to `setup.py` so that the package can be pip-installed at once.

## Wiki 抽取状态

- 该 issue 不在当前 operator/kernel case 表中；保留索引，但暂不推断优化结论。
- 后续迭代应在可用时读取完整讨论评论。
