# bitwise-30018

- upstream id: 30018
- source type: pr
- mechanism: batch_invariance
- current status: stable
- decision: include
- priority: high
- last reviewed: 2026-06-23

## Why stable

PR merged（2025-12-09），human approved（yewentao256）。Changes 极为轻量（3 files, +23/−3）。扩展 batch-invariant matmul override 从 SM100 到 SM80/SM89。已有 regression test（tests/v1/determinism/test_batch_invariance.py），1000 reqs 从 28-36 distinct outputs 收敛到 1。

## Boundary

- No CUDA graph support（必须 `enforce_eager`）。Post-merge 2026-04-08 确认。
- ~50% throughput reduction。
- FA2 `num_splits` 曾被 review 指出会破坏 CUDA graph metadata，最终版本已 revert 该改动。
- LoRA split_k=1 含 review support（jeejeelee "LGTM"）但 landed-code 子路径未独立验证。

## Evidence summary

Follows PR #27433 的 batch-invariant 框架。核心改动：`batch_invariant.py` 中 SM capability check 从 `is_device_capability(100)` 扩展为 80/89/100。Test 中 `enforce_eager=True` for SM<90，`enable_prefix_caching=False`。

Review 轨迹：initial commit 含 FA2 num_splits 改动被 bot 指出 CUDA graph breaking issue，author 在 `4aa591e` 中 revert。最终 merged diff 仅 3 个文件。Human reviewer yewentao256 approved。

Target evidence: PR #30018 files: batch_invariant.py, utils.py, test_batch_invariance.py; review approval from yewentao256; post-merge question about enforce_eager from ProExpertProg.
