# bitwise-42903

- upstream id: 42903
- source type: pr
- mechanism: kv_cache_identity
- current status: defer_blocked
- decision: defer
- priority: medium
- last reviewed: 2026-06-22

## Why not promoted

Open PR (5 comments, 12 review comments, 2 changed files); eager-mode SCO store+load paths have duplicate gpu_block_ids; load path must keep latest because each logical position has different KV data. Repro: DeepSeek-V4-Flash (sliding window) + SimpleCPUOffloadConnector + hybrid-kv-cache-manager + prefix-caching + async-scheduling.

## Evidence summary

新增索引：offload identity 的 block id dedup 维度——store/load 队列中同一 logical position 可重复出现，load path 必须保留最新。与 #31210/#31341 offload 时序机制交叉但关注点不同。PR review 仍在进行（12 review comments）。

Target evidence: PR #42903 snippet: gpu_block_ids dedup in store+load paths; DeepSeek-V4-Flash sliding window; SimpleCPUOffloadConnector; long-context conversational continuation; deterministic repro

## Missing evidence

Open PR (5 comments, 12 review comments, 2 changed files); eager-mode SCO store+load paths have duplicate gpu_block_ids; load path must keep latest because each logical position has different KV data. Repro: DeepSeek-V4-Flash (sliding window) + SimpleCPUOffloadConnector + hybrid-kv-cache-manager + prefix-caching + async-scheduling.

## Next action

追踪 #42903 是否合并；若合并确认 gpu_block_ids dedup 是否覆盖 store 和 load 两条路径。
