# vllm-project/vllm#17516: [Bug]:

| 字段 | 值 |
| --- | --- |
| Issue | [#17516](https://github.com/vllm-project/vllm/issues/17516) |
| 状态 | closed |
| 标签 | bug;stale |
| 评论 | 2; 本地原始数据只有评论数量，没有评论正文 |
| 一级分类 | development |
| 工作域 | ci_build;distributed_parallel;frontend_api;hardware_porting;model_support;speculative_decoding |
| 子分类 | env_compat |
| Operator 关键词 | cuda;operator;triton |
| 症状 | build_error |
| 根因提示 | env_dependency |
| 硬件范围 | amd;nvidia |
| 需要人工复核 | False |

## 源证据

### Issue 标题

> [Bug]:

### Issue 正文摘录

### Your current environment ### 🐛 Describe the bug I’m using vLLM via the OpenAI-compatible HTTP endpoint (/v1/chat/completions), sending requests via aiohttp. I passed a list of stop strings (e.g., ["\ "]) via the 'stop' arg, but in one case, the response returned a stop_reason that was not one of those strings, and instead is "151643". This appears to be some internal ID or token, and not an expected value for stop_reason. The model I'm using is 'Qwen2.5-7B-Instruct-1M'. This has only happened once on me and I haven't been able to reproduce it, but reporting in case it indicates a rare bug in how stop_reason is mapped or exposed in the OpenAI-compatible API. Args you may find useful for reproduction: { "stop": ["\ "], "include_stop_str_in_output": True, } I also tried to decode that value as a token id and compare with the eos token: \>\>\> tokenizer.decode([151643], clean_up_tokenization_spaces=False) ' ' \>\>\> tokenizer.eos_token ' ' While meaningful, it doesn't make this stop behavior reasonable. As I guess eos token and the stop strings should be the only reasons that can make the generation stop? ### Before submitting a new issue... - [x] Make sure you already searched fo...

## 候选优化模式

- [Scheduler 与请求状态生命周期](../patterns/scheduler_request_lifecycle.md) - 分数 4: [Bug]: bug;stale ### Your current environment ### 🐛 Describe the bug I’m using vLLM via the OpenAI-compatible HTTP endpoint (/v1/chat/completions), sending requests via aiohttp. I passed a list of stop strings (e.g., ["...
- [构建、依赖与打包](../patterns/build_dependency_packaging.md) - 分数 3: test/), which can answer lots of frequently asked questions. development ci_build;distributed_parallel;frontend_api;hardware_porting;model_support;speculative_decoding cuda;operator;triton build_error env_dependency You...
- [硬件架构 Guard](../patterns/hardware_arch_guard.md) - 分数 2: op? ### Before submitting a new issue... - [x] Make sure you already searched for relevant issues, and asked the chatbot living at the bottom right corner of the [documentation page](https://docs.vllm.ai/en/latest/), wh...
- [模型格式与 Adapter 路径](../patterns/model_format_adapter.md) - 分数 2: e internal ID or token, and not an expected value for stop_reason. The model I'm using is 'Qwen2.5-7B-Instruct-1M'. This has only happened once on me and I haven't been able to reproduce it, but reporting in case it ind...
- [Backend 路由与 Fallback](../patterns/backend_routing_fallback.md) - 分数 1: nd_api;hardware_porting;model_support;speculative_decoding cuda;operator;triton build_error env_dependency Your current environment

## Wiki 抽取状态

- 风险：该 issue 有评论，但本地数据只有评论数量，没有评论正文。
- 本地没有 linked-fix 证据；目前只支持症状/路径抽取。
- 后续迭代应在可用时读取完整讨论评论。
