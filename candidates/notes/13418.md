# bitwise-13418
- upstream id: 13418
- source type: issue
- mechanism: verification_contract
- current status: defer_blocked
- decision: defer
- priority: medium
- last reviewed: 2026-06-23

## Why not promoted
Issue is closed, but local evidence does not expose a direct linked fix. The only PR numbers extracted into the case page are clearly stack-trace false positives (`#4`, `#6`, `#7`, `#12`, `#16`), and the local snapshot has no comment bodies or changed files tying the padding / cudagraph replay hang to a landed patch. Keep as a blocked graph-padding boundary item.

## Evidence summary
Reporter isolates a V1 cudagraph replay hang when `num_scheduled_tokens` is padded up to a captured size and the first active token count is not aligned to 8. The failure only appears in the padded replay path, which makes it relevant to the broader “captured inputs must keep valid padded state / shape contract” family, but current local evidence only supports the symptom and reproduction, not a closure.
