# bitwise-30872
- upstream id: 30872
- source type: issue_pr_pair
- mechanism: deterministic_dispatch_reduction
- current status: include_with_boundary
- decision: include
- priority: medium
- last reviewed: 2026-06-23

## Why not promoted
Closed issue with linked bugfix PR #32836, but the current local closure summary is still high-level. It is enough to keep as a bounded SM100 LoRA kernel workaround item, not enough for stable promotion.

## Evidence summary
The issue reports a Triton kernel compilation failure for LoRA on SM100 that only manifests on first request, and the linked PR says it fixes the issue by adding an env var to control PDL in LoRA. That is a hardware-specific kernel-dispatch / control-path fix, which fits the deterministic-dispatch boundary family more than a stable cross-platform mechanism.
