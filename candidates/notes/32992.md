# bitwise-32992
- upstream id: 32992
- source type: issue_pr_pair
- mechanism: batch_invariance
- current status: include_with_boundary
- decision: include
- priority: high
- last reviewed: 2026-06-23

## Why not promoted
Closed issue with linked bugfix PR #33383. The local case page and PR body consistently say the fix is to disable `torch.compile` for batch invariance on Blackwell, but the targeted PR snapshot does not give a reliable merged-state signal in the fields we keep locally. Treat as a strong landed-looking boundary item until merge closure is re-confirmed from a source we trust for promotion.

## Evidence summary
The issue reports that `VLLM_BATCH_INVARIANT=1` passes on Hopper but fails on B200 only when `torch.compile` is active; `enforce_eager=True` restores determinism. PR #33383 says it fixes #32992 by disabling `torch.compile` for batch invariance on Blackwell and lists before/after determinism tests across FLASH_ATTN and FLASHINFER. The mechanism fits batch-invariance support-gate behavior rather than a generic math fix.
