# bitwise-43559
- upstream id: 43559
- source type: issue_pr_pair
- mechanism: prefix_cache_equivalence
- current status: defer_blocked
- decision: include
- priority: high
- last reviewed: 2026-06-23

## Why defer_blocked
MTP + prefix caching causes ~20% accuracy drop (prefix caching alone and MTP alone are both fine — only the combination degrades). Root cause: in full attention with spec decode, the final block is dropped (contains partial accepted tokens), but mamba does not drop the final block, causing stale state corruption. PR #43650 (closes_keyword, confidence 0.95) fixes mamba final block drop. Issue open; fix not yet confirmed merged.

## Evidence summary
Issue open, 8 comments. 4-way comparison: base/base+pc/mtp/mtp+pc — only mtp+pc degrades. PR #43650 "MTP + enable prefix caching + mamba accuracy fix."

## Next action
Verify if PR #43650 is merged; if confirmed landed, promote to stable.
