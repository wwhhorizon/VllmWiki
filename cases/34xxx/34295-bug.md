# vllm-project/vllm#34295: [Bug]:

| 字段 | 值 |
| --- | --- |
| Issue | [#34295](https://github.com/vllm-project/vllm/issues/34295) |
| 状态 | open |
| 标签 | bug;stale |
| 评论 | 2; 本地原始数据只有评论数量，没有评论正文 |
| 一级分类 | correctness |
| 工作域 | ci_build;distributed_parallel;frontend_api;gemm_linear;hardware_porting;model_support;sampling_logits;speculative_decoding |
| 子分类 | precision |
| Operator 关键词 | cuda;operator;sampling;triton |
| 症状 | build_error;nan_inf |
| 根因提示 | dtype;env_dependency |
| 硬件范围 | amd;nvidia |
| 需要人工复核 | False |

## 源证据

### Issue 标题

> [Bug]:

### Issue 正文摘录

### Your current environment ### 🐛 Describe the bug When generating real numbers from a model and using structured outputs to constrain the values to the range [-1, 1], all negative values are -1. If the range is changed to [-0.9999, 1], however, negative decimals are produced properly. A request like this: ``` POST /v1/chat/completions Authorization: Bearer key Content-Type: application/json { "model": "a-model", "messages": [ { "role": "system", "content": "You are a random number generator." }, { "role": "user", "content": "You must output an array of random real numbers between -1 and 1. There must be at least one positive number and at least one negative number." } ], "response_format": { "type": "json_schema", "json_schema": { "name": "output_schema", "strict": true, "schema": { "type": "object", "properties": { "numbers": { "type": "array", "items": { "type": "number", "minimum": -1, "maximum": 1 }, "minItems": 10, "maxItems": 10 } }, "required": [ "numbers" ], "additionalProperties": false } } } } ``` Returns this: ```json { "index": 0, "message": { "role": "assistant", "content": "{\"numbers\":[-1.0,0.999998,0.125495,-1.0,-1.0,0.803543,-1.0,-1.0,0.697396,0.002826]}", "ref...

## 候选优化模式

- [构建、依赖与打包](../patterns/build_dependency_packaging.md) - 分数 3: es are -1. If the range is changed to [-0.9999, 1], however, negative decimals are produced properly. A request like this: ``` POST /v1/chat/completions Authorization: Bearer key Content-Type: application/json { "model"...
- [硬件架构 Guard](../patterns/hardware_arch_guard.md) - 分数 3: ": null, "token_ids": null } ``` Other notes - If I specify a smaller minimum value like -1.5, negative values become -1. - If I specify maximum only, negative values are correct yet unconstrained. - If I specify minimu...
- [Scheduler 与请求状态生命周期](../patterns/scheduler_request_lifecycle.md) - 分数 3: [Bug]: bug;stale ### Your current environment ### 🐛 Describe the bug When generating real numbers from a model and using structured outputs to constrain the values to the range [-1, 1], all negative values are -1. If th...
- [模型格式与 Adapter 路径](../patterns/model_format_adapter.md) - 分数 2: ironment ### 🐛 Describe the bug When generating real numbers from a model and using structured outputs to constrain the values to the range [-1, 1], all negative values are -1. If the range is changed to [-0.9999, 1], h...
- [Backend 路由与 Fallback](../patterns/backend_routing_fallback.md) - 分数 1: odel_support;sampling_logits;speculative_decoding cuda;operator;sampling;triton build_error;nan_inf dtype;env_dependency Your current environment

## Wiki 抽取状态

- 风险：该 issue 有评论，但本地数据只有评论数量，没有评论正文。
- 本地没有 linked-fix 证据；目前只支持症状/路径抽取。
- 后续迭代应在可用时读取完整讨论评论。
