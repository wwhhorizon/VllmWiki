# bitwise-33537

- upstream id: 33537
- source type: pr
- mechanism: deterministic_dispatch_reduction
- current status: include_with_boundary
- decision: include
- priority: medium
- last reviewed: 2026-06-19

## Why not promoted

PR 仍 open/stale；单测验证 warmup framework，但 reviewer 要求的具体 main-branch repro 后续主要变成首请求 latency variability，不是 token/logprob bitwise divergence；不能 promotion 为已修复 bitwise bug。

## Evidence summary

新增 insight：warmup 是 cold-start serving-state boundary；graph/JIT/cache warming 可能影响第一真实请求，但 latency predictability、compile completion 和 token/logprob bitwise equality 是三种不同证据。

Target evidence: PR #33537 files: batch_invariant.py warmup iterations, gpu_worker.py warmup hook, test_determinism_warmup.py 12 tests; comments on DeepSeek-V3 TRITON_MLA first-request latency 8714/6047/3186ms -> 3309/3236/3257ms

## Missing evidence

PR 仍 open/stale；单测验证 warmup framework，但 reviewer 要求的具体 main-branch repro 后续主要变成首请求 latency variability，不是 token/logprob bitwise divergence；不能 promotion 为已修复 bitwise bug。

## Next action

若继续推进，寻找 first-request token/logprob divergence 的 before/after；否则保持 latency/warmup boundary。
