# vllm-project/vllm#18210: [Usage]: Unable to customize FastAPI/Uvicorn log format for OpenAI compatible server

| 字段 | 值 |
| --- | --- |
| Issue | [#18210](https://github.com/vllm-project/vllm/issues/18210) |
| 状态 | closed |
| 标签 | usage |
| 评论 | 1; 本地原始数据只有评论数量，没有评论正文 |

## 源证据

### Issue 标题

> [Usage]: Unable to customize FastAPI/Uvicorn log format for OpenAI compatible server

### Issue 正文摘录

I am trying to set a custom log format for all logs generated by the vLLM server when exposing an OpenAI-compatible endpoint. This includes both the initial vLLM setup logs and, most important: the subsequent logs **from the FastAPI/Uvicorn** server (when there is a request) I took a good look at the [doc](https://docs.vllm.ai/en/latest/getting_started/examples/logging_configuration.html#examples) on custom logging but for the uvicorn server it seems you can only change the log level cf `--uvicorn-log-level` arg, but not set a custom config for uvicorn. Currently, I have defined a logger config that properly format the vLLM **other than** the `uvicorn`/`fastAPI` server. See json config below. ```json { "version": 1, "disable_existing_loggers": false, "formatters": { "plain": { "class": "logging.Formatter", "format": "%(asctime)s [%(levelname)s] %(filename)s:%(lineno)d - %(funcName)s() - %(message)s", "datefmt": "%d-%m-%Y %H:%M:%S" } }, "handlers": { "console_custom": { "class": "logging.StreamHandler", "formatter": "plain", "level": "INFO", "stream": "ext://sys.stdout" }, "file_custom": { "class": "logging.FileHandler", "formatter": "plain", "level": "INFO", "filename": "/var/log/...

## 候选优化模式

- [构建、依赖与打包](../patterns/build_dependency_packaging.md) - 分数 2: tible endpoint. This includes both the initial vLLM setup logs and, most important: the subsequent logs **from the FastAPI/Uvicorn** server (when there is a request) I took a good look at the [doc](https://docs.vllm.ai/...
- [模型格式与 Adapter 路径](../patterns/model_format_adapter.md) - 分数 2: [Usage]: Unable to customize FastAPI/Uvicorn log format for OpenAI compatible server usage I am trying to set a custom log format for all logs generated by the vLLM server when exposing an OpenAI-compatible endpoint. Th...
- [硬件架构 Guard](../patterns/hardware_arch_guard.md) - 分数 1: llm ### Before submitting a new issue... - [x] Make sure you already searched for relevant issues, and asked the chatbot living at the bottom right corner of the [documentation page](https://docs.vllm.ai/en/latest/), wh...
- [Metadata 与 Layout 契约](../patterns/metadata_layout_contract.md) - 分数 1: config below. ```json { "version": 1, "disable_existing_loggers": false, "formatters": { "plain": { "class": "logging.Formatter", "format": "%(asctime)s [%(levelname)s] %(filename)s:%(lineno)d - %(funcName)s() - %(messa...
- [Scheduler 与请求状态生命周期](../patterns/scheduler_request_lifecycle.md) - 分数 1: the subsequent logs **from the FastAPI/Uvicorn** server (when there is a request) I took a good look at the [doc](https://docs.vllm.ai/en/latest/getting_started/examples/logging_configuration.html#examples) on custom lo...

## Wiki 抽取状态

- 风险：该 issue 有评论，但本地数据只有评论数量，没有评论正文。
- 该 issue 不在当前 operator/kernel case 表中；保留索引，但暂不推断优化结论。
- 后续迭代应在可用时读取完整讨论评论。
