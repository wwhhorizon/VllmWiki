# bitwise-43301
- upstream id: 43301
- source type: issue
- mechanism: verification_contract
- current status: include_with_boundary
- decision: include
- priority: high
- last reviewed: 2026-06-23

## Why include_with_boundary
Accuracy divergence between mamba_chunk_scan_combined_varlen and selective_state_update kernels — two algebraically equivalent SSM kernels produce different bf16 outputs due to different reduction orders (bf16 matmul with intermediate casts vs fp32 throughout). Directly verification_contract: kernel-level numerical divergence between mathematically equivalent paths. PR #43186 only lowered the gsm8k accuracy threshold (CI workaround), #42430 triggered the reroute. Issue is an investigation request, not a landed fix. Boundary: the insight about reduction-order divergence is stable, but no fix or tolerance recommendation has landed.

## Evidence summary
Issue open, 1 comment. PR #42430 rerouted D-side 1-token recompute from mamba_chunk_scan to SSU. PR #43186 lowered granite-4.0-h-tiny gsm8k threshold. Investigation goal: quantify divergence, test hardware dependence (A100/H100), recommend fix or tolerance.

## Next action
Monitor for a follow-up PR that addresses the kernel numerical divergence or establishes an acceptable tolerance.
