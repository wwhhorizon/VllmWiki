# vllm-project/vllm#1182: Parallel Requests in AsyncLLMEngine giving different results even when using argmax

| 字段 | 值 |
| --- | --- |
| Issue | [#1182](https://github.com/vllm-project/vllm/issues/1182) |
| 状态 | closed |
| 标签 |  |
| 评论 | 4; 本地原始数据只有评论数量，没有评论正文 |

## 源证据

### Issue 标题

> Parallel Requests in AsyncLLMEngine giving different results even when using argmax

### Issue 正文摘录

Hi, I was experimenting with the [vllm server](https://github.com/vllm-project/vllm/blob/main/vllm/entrypoints/api_server.py), and found an interesting issue. I checked the issues and couldn't find raised issues that are related to this. Other issues are comparing the difference in output between vLLM and HuggingFace, while this issue is about comparing the difference in outputs generated by vLLM itself at different times even when setting `temperature=0`, `top_p=1`, and `top_k=-1`. --------------------------------------------- TL;DR: Why vLLM give different responses when we're sending the same input data and the same input prompt when we're sending **parallel requests/batch requests** multiple times? While it's always giving the same response when sending a **single request** multiple times? --------------------------------------------- Experiment setup: - Model: fine-tuned llama-2 7B and 13B - We’re testing with a QnA task using ~700 query & knowledge pairs. - For parallel requests testing, we’re sampling 3 pairs randomly in each iteration. - Thus, each query may have been sampled more than once and we’re interested in examining whether multiple answers from the same query are...

## 候选优化模式

- [模型格式与 Adapter 路径](../patterns/model_format_adapter.md) - 分数 3: is. Other issues are comparing the difference in output between vLLM and HuggingFace, while this issue is about comparing the difference in outputs generated by vLLM itself at different times even when setting `temperat...
- [验证与 Benchmark](../patterns/verification_benchmarking.md) - 分数 2: ---- Experiment setup: - Model: fine-tuned llama-2 7B and 13B - We’re testing with a QnA task using ~700 query & knowledge pairs. - For parallel requests testing, we’re sampling 3 pairs randomly in each iteration. - Thu...
- [Bitwise 确定性与数值等价](../patterns/bitwise_determinism_equivalence.md) - 分数 1: from the same query are the exact same or not. - Hence, we’re testing deterministic behavior. - For single requests testing, we’re sending only the same sample one after another (not in parallel) - We’re also interested...
- [Scheduler 与请求状态生命周期](../patterns/scheduler_request_lifecycle.md) - 分数 1: Parallel Requests in AsyncLLMEngine giving different results even when using argmax Hi, I was experimenting with the [vllm server](https://github.com/vllm-project/vllm/blob/main/vllm/entrypoints/api_server.py), and foun...

## Wiki 抽取状态

- 风险：该 issue 有评论，但本地数据只有评论数量，没有评论正文。
- 该 issue 不在当前 operator/kernel case 表中；保留索引，但暂不推断优化结论。
- 后续迭代应在可用时读取完整讨论评论。
