# bitwise-41651

- upstream id: 41651
- source type: issue_pr_pair
- mechanism: deterministic_dispatch_reduction
- current status: stable
- decision: include
- priority: high
- last reviewed: 2026-06-21

## Why not promoted

已 merged；核心机制是 `#42650` 修 non-uniform per-layer Q-head metadata mismatch，把 plan-time head count/source of truth 从 model-wide config 收敛到 served layer 的 `impl.num_heads`。`#42580` 和 Gemma4 MTP grouping 风险属于后续独立边界，不否定这条 metadata 修复本身。

## Evidence summary

新增 insight：attention metadata builder 的 plan-time head count、scratch shape 和 runtime q.shape 必须来自同一 per-layer source of truth；model-wide config 会在非均匀 head 模型上制造 deterministic garbage。后续独立 scale/grouping 问题应拆开追踪，而不是回流污染这条已 merged 核心机制。

Target evidence: Issue #41651 comments; PR #42650 files: flashinfer.py/triton_attn.py use get_num_attention_heads_from_layers; utils.py reads layer.impl.num_heads and asserts group uniformity; DomBrown before/after needle test comment; later comment on Gemma4 MTP target/draft num_heads grouping crash

## Missing evidence

无阻塞性缺口；只保留适用边界和后续回归观察。

## Next action

主机制已稳定；主机制已稳定；#42580 已于 2026-05-21 被作者关闭（被已合并的 #42080 替代），不再单独追踪；后续只追踪 Gemma4 MTP grouping key。
