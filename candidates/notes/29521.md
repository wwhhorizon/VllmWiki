# bitwise-29521
- upstream id: 29521
- source type: issue_pr_pair
- mechanism: verification_contract
- current status: include_with_boundary
- decision: include
- priority: medium
- last reviewed: 2026-06-23

## Why not promoted
Closed CI issue with linked PRs #31076 and #31077, but the local evidence clusters together multiple failing sampler behaviors on ROCm. Useful as a narrowed regression bucket, not yet a single stable mechanism claim.

## Evidence summary
The failing tests cover beam-search concurrency consistency and logprob rank checks on AMD CI. The linked PRs point to ROCm CUDA-graph replay synchronization and disabling CUDA graphs for a specific model path, which makes this a verification-contract / regression-test boundary: the observable contract is deterministic sampler behavior, while the underlying landed fixes still span more than one failure surface.

