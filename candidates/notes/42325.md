# bitwise-42325

- upstream id: 42325
- source type: issue_pr_pair
- mechanism: quant_dtype_semantics
- current status: stable
- decision: include
- priority: medium
- last reviewed: 2026-06-19

## Why not promoted

后续评论质疑 Python IR 是否可作为 CUDA spec；因此结论边界是 PR #42379 已合并的 native-dtype behavior 与测试覆盖，不扩展成通用规范。

## Evidence summary

新增 insight：regular RMSNorm 与 fused FP8 quant RMSNorm 必须共享同一个 multiply dtype/reference boundary；修 fused path 时要同步 regular/composite path，避免两条路径漂移。

Target evidence: Issue #42325 body/comments; PR #42379 merged files: layernorm_kernels.cu and layernorm_quant_kernels.cu; before/after max diff 3.125e-02 -> 0; 865 core layernorm tests and 1442 IR layernorm tests; TinyLlama/H100 lm_eval no regression

## Missing evidence

无阻塞性缺口；只保留适用边界和后续回归观察。

## Next action

后续若 CUDA spec 讨论反转为 FP32 行为，需要同步更新 reference boundary。
