# bitwise-42898
- upstream id: 42898
- source type: issue
- mechanism: quant_dtype_semantics
- current status: defer_blocked
- decision: include
- priority: high
- last reviewed: 2026-06-23

## Why defer_blocked
Qwen3.5 NVFP4 disaggregated serving (NIXL) becomes accuracy-unstable across repeated identical GSM8K runs in same process — first run healthy, second run degrades sharply. Directly quant_dtype_semantics + kv_cache_identity: disagg KV transfer or SSM state accumulates errors across runs. No linked PR, no maintainer closure. Issue open, 4 comments. Prefix caching disabled, so not a prefix cache issue — points to SSM conv state or NIXL transfer identity.

## Evidence summary
Issue open. Topology: 2xTEP8 prefill, 1xDEP8 decode, BF16 KV, HMA enabled, SSM conv state layout DS. No error logs, successful NIXL transfers. Accuracy degradation only on second identical run.

## Next action
Collect missing evidence: check if any PR addresses SSM state cleanup or NIXL transfer identity between runs; track for maintainer resolution.
