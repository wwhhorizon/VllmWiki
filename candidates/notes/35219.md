# bitwise-35219

- upstream id: 35219
- source type: pr
- mechanism: kv_cache_identity
- current status: stable
- decision: include
- priority: high
- last reviewed: 2026-06-20

## Why not promoted

已 merged；核心机制是 hybrid Mamba/attention 共享 pool 中，新分配给 FullAttentionSpec 的 block 必须在使用前清掉旧 fp32 Mamba state bits；边界是其 narrow hybrid scope，而不是通用 KV zeroing。

## Evidence summary

新增 insight：KV block identity 还包括上一生命周期 dtype 解释；共享 block pool 中旧 fp32 SSM state bits 可在 attention KV dtype 下变成 NaN/Inf，并通过 mask kernel 污染其他请求。适用范围窄并不妨碍这条 merged 核心机制稳定成立。

Target evidence: PR #35219 files: KVCacheConfig.has_mamba_layers/needs_kv_cache_zeroing; SingleTypeKVCacheManager tracks new FullAttentionSpec block ids; SchedulerOutput carries new_block_ids_to_zero; GPUModelRunner calls KVBlockZeroer Triton kernel; PR body/reviews discuss fp32 Mamba state reused as fp8/fp16 attention KV and 0*NaN mask propagation

## Missing evidence

无阻塞性缺口；只保留适用边界和后续回归观察。

## Next action

主机制已稳定；后续只在 Mamba/hybrid 相关新问题中区分 cross-dtype stale NaN、metadata pointer、spec 子类 zeroing 和 MTP kernel-selection。
