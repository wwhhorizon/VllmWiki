# bitwise-39273
- upstream id: 39273
- source type: issue_pr_pair
- mechanism: kv_cache_identity
- current status: defer_blocked
- decision: include
- priority: high
- last reviewed: 2026-06-23

## Why defer_blocked
Ngram speculative decoding produces corrupted output on hybrid GDN (Qwen3.5) models — symptom tagged nondeterministic. Root cause: GDN conv + SSM state corruption when ngram spec decode miscomputes which SSM state block to copy (num_tokens_running_state / new_num_computed_tokens miscalculation). PR #40738 (closes_keyword, confidence 0.95) fixes the state corruption. Issue still open; fix not yet confirmed merged/landed.

## Evidence summary
Issue open, 9 comments. Without ngram: correct output. With ngram: corrupted output. PR #40738 "Fix GDN conv + SSM state corruption with ngram spec decode." Affects hybrid GDN + full attention models with FP8 compressed-tensors.

## Next action
Verify if PR #40738 is merged; if confirmed landed with test coverage, promote to stable.
