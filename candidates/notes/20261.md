# bitwise-20261
- upstream id: 20261
- source type: issue_pr_pair
- mechanism: prefix_cache_equivalence
- current status: defer_blocked
- decision: include
- priority: high
- last reviewed: 2026-06-23

## Why defer_blocked
Prefix caching ignores visual input → incorrect multimodal outputs under concurrency (garbled/truncated tokens). Cache key does not correctly account for multimodal image content. PR #36622 (closes_keyword, confidence 0.95) "Fix off-by-one in multimodal prefix cache hash boundary check." Issue still open; fix not yet confirmed merged.

## Evidence summary
Issue open (bug;unstale), 16 comments. Disabling prefix caching resolves. PR #36622 (closes_keyword) fixes off-by-one in multimodal prefix cache hash boundary. Identical text prompts with different images trigger incorrect cache hits.

## Next action
Verify if PR #36622 is merged; if confirmed landed, promote to stable (direct fix for cache identity boundary).
