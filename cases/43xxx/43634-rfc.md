# vllm-project/vllm#43634: [RFC]:

| 字段 | 值 |
| --- | --- |
| Issue | [#43634](https://github.com/vllm-project/vllm/issues/43634) |
| 状态 | open |
| 标签 | RFC |
| 评论 | 0; 本地原始数据只有评论数量，没有评论正文 |
| 一级分类 | performance |
| 工作域 | activation_norm;attention_kv_cache;distributed_parallel;gemm_linear;model_support;multimodal_vlm;quantization;sampling_logits |
| 子分类 | memory |
| Operator 关键词 | activation;attention;cuda;kernel;operator |
| 症状 | oom;slowdown |
| 根因提示 | env_dependency;memory_layout;shape |
| 硬件范围 | nvidia |
| 需要人工复核 | False |

## 源证据

### Issue 标题

> [RFC]:

### Issue 正文摘录

### Motivation. # RFC: Scalable Sequence Parallelism (SP) for Vision Transformers (ViT) in VLM Inference ## Motivation As Vision-Language Models (VLMs) evolve to process higher-resolution images, video frames, and ultra-long multi-modal contexts, the computational and memory footprint of the visual processing stage—predominantly governed by Vision Transformers (ViTs)—has become a critical bottleneck in distributed inference. While modern inference frameworks have successfully laid the groundwork for managing long textual sequences in the Large Language Model (LLM) backbone via sequence-level distribution, the multi-modal visual ingestion pipeline remains highly unoptimized in tensor-parallel (TP) environments. Currently, when handling large visual inputs, the implementation inherits severe performance degradation due to the following structural limitations: 1. **Serial Execution Bottleneck in Multi-GPU Environments:** Although the textual component can leverage distributed prefill parallelization, the long sequence of visual tokens generated by the ViT is often processed without natively aligning to the Sequence Parallelism (SP) boundaries of the downstream LLM. This leads to seve...

## 候选优化模式

- [模型格式与 Adapter 路径](../patterns/model_format_adapter.md) - 分数 5: RFC: Scalable Sequence Parallelism (SP) for Vision Transformers (ViT) in VLM Inference ## Motivation As Vision-Language Models (VLMs) evolve to process higher-resolution images, video frames, and ultra-long multi-modal...
- [Metadata 与 Layout 契约](../patterns/metadata_layout_contract.md) - 分数 4: [S/P, C] + pos_embeds[local_slice] → [S/P, 1, C] [4] For EACH ViT Block: ┌─ Pre-Attention (local): Norm → QKV proj → [S/P, 1, num_heads, head_dim] │ │ ★ All-to-All #1: (seq_local, heads_all) → (seq_full, heads/P) │ Befo...
- [构建、依赖与打包](../patterns/build_dependency_packaging.md) - 分数 3: sm (TP) configurations, heavy communication overhead is incurred when slicing the attention weights of large Vision layers across hosts. Furthermore, redundant intermediate states or duplicated visual tokens are often r...
- [硬件架构 Guard](../patterns/hardware_arch_guard.md) - 分数 3: [RFC]: RFC ### Motivation. # RFC: Scalable Sequence Parallelism (SP) for Vision Transformers (ViT) in VLM Inference ## Motivation As Vision-Language Models (VLMs) evolve to process higher-resolution images, video frames...
- [Backend 路由与 Fallback](../patterns/backend_routing_fallback.md) - 分数 2: ore: 1. **Zero changes** to the attention kernel (`MMEncoderAttention`, FlashAttention, etc.). 2. **Existing TP linear layers** operate correctly; only a flag is added to skip redundant all-reduce in `RowParallelLinear`...

## Wiki 抽取状态

- 本地没有 linked-fix 证据；目前只支持症状/路径抽取。
- 后续迭代应在可用时读取完整讨论评论。
