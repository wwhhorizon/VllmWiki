# bitwise-25404

- upstream id: 25404
- source type: issue_pr_pair
- mechanism: deterministic_dispatch_reduction
- current status: stable
- decision: include
- priority: high
- last reviewed: 2026-06-19

## Why not promoted

已 merged 的 PR #25603 只是第一批 batch-invariant override plumbing；不能外推为完整 per-kernel config 系统或全 serving path 已 batch invariant。

## Evidence summary

新增 insight：review 将 per-kernel deterministic override 需求收敛为全局 batch-invariant mode 的首批 plumbing；验证需要 token output 与 logprob bit equality，hook 存在不等于每个 kernel 都已覆盖。

Target evidence: Issue #25404; PR #25603 merged files: csrc/core/batch_invariant.hpp, batch_invariant.py, layernorm/fused quant/topk/flex attention hooks, gpu_model_runner init, test_batch_invariance.py; review comments on naming batch invariant vs deterministic and logprob equality

## Missing evidence

无阻塞性缺口；只保留适用边界和后续回归观察。

## Next action

后续追踪更多 kernels 是否接入 VLLM_BATCH_INVARIANT；检查 multidim torch.sum/mean deterministic reduction comment 是否在后续 PR 闭环。
