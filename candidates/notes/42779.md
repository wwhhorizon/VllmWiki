# bitwise-42779

- upstream id: 42779
- source type: pr
- mechanism: verification_contract
- current status: include_with_boundary
- decision: include
- priority: medium
- last reviewed: 2026-06-22

## Why not promoted

Open PR (5 comments, 0 review comments, 2 changed files); zeros padded model inputs (input_ids and positions padding area) before execution and asserts with torch.equal that padding is zero.

## Evidence summary

新增 insight：padded inputs 的 stale data 是 deterministic 输出的潜在污染源；CUDA graph replay 或 padded GEMM 可能读到非零 stale 值。清零是防护手段，torch.equal 断言是验证契约。PR 仍 open，写成 include_with_boundary。

Target evidence: PR #42779 snippet: assert torch.equal(input_ids[num_input_tokens:], torch.zeros(...)); assert torch.equal(positions[num_input_tokens:], torch.zeros(...))

## Missing evidence

Open PR (5 comments, 0 review comments, 2 changed files); zeros padded model inputs (input_ids and positions padding area) before execution and asserts with torch.equal that padding is zero.

## Next action

追踪 #42779 是否合并；若合并可作为 padded inputs 必须清零这一 verification contract 的 landed 样例。
