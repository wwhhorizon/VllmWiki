# bitwise-16337
- upstream id: 16337
- source type: issue_pr_pair
- mechanism: quant_dtype_semantics
- current status: defer_blocked
- decision: include
- priority: medium
- last reviewed: 2026-06-23

## Why defer_blocked
FP8 quantization + enforce_eager=False (CUDA graph padding) causes gibberish output on Llama-4-Scout. CUDA graph padding executes padded positions beyond total_num_scheduled_tokens, causing FP8 quantized model to produce garbage. PR #42779 (closes_keyword, confidence 0.95) "Zero padded model inputs before execution." Issue closed/stale; fix not yet confirmed merged with test coverage.

## Evidence summary
Issue closed (bug;torch.compile;stale), 10 comments. enforce_eager=True produces correct output. Removing CUDA graph padding in gpu_model_runner.py resolves. PR #42779 zeros padded model inputs in _preprocess.

## Next action
Verify if PR #42779 is merged; if confirmed landed, promote to stable.
