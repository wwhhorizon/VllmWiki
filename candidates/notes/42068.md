# bitwise-42068
- upstream id: 42068
- source type: issue_pr_pair
- mechanism: batch_invariance
- current status: include_with_boundary
- decision: include
- priority: medium
- last reviewed: 2026-06-23

## Why not promoted
Closed issue with a maintainer explanation and a closing comment pointing to PR #39930, but the local targeted corpus does not include that PR's changed files or merged snapshot. This is strong enough to keep as a narrowed boundary item, not strong enough for stable promotion.

## Evidence summary
The issue says Gemma 4 target-side backend locking for heterogeneous head dimensions was being propagated to an independent DFlash drafter, forcing `TRITON_ATTN` onto a non-causal draft path that needs `FLASHINFER` or `FLASH_ATTN`. A maintainer comment explicitly distinguishes the expected target-side Gemma 4 constraint from the independent-drafter case, and the closing comment says the issue was fixed by PR #39930. Mechanistically this fits a backend/support-gate contract: KV-shared MTP drafters may need target backend inheritance, independent drafters must be allowed to auto-select a non-causal-capable backend.
