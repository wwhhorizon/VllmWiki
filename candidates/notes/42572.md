# bitwise-42572
- upstream id: 42572
- source type: issue
- mechanism: kv_cache_identity
- current status: defer_blocked
- decision: include
- priority: high
- last reviewed: 2026-06-23

## Why defer_blocked
Intermittent vectorized_gather_kernel index out of bounds in triton_reshape_and_cache_flash (KV cache write path) during Gemma4 31B + MTP K=3. ~8% crash rate across cells/GPUs/quantizations — strongly non-deterministic. Scheduler/spec-decode layer produces slot mapping with out-of-range block index. PR #42638 (mentioned) fixes expand_batch_to_tokens on CPU only, not the GPU path. No direct landed fix for the GPU slot mapping race. Issue open.

## Evidence summary
Issue open, 1 comment. Same family as closed #28785 (Qwen3-VL). Affects both FP8-block and NVFP4 quantizations. Crash in unified_kv_cache_update → triton_reshape_and_cache_flash. PR #42638 is CPU-only fix, related but not direct closure.

## Next action
Track for a GPU-path fix to the spec-decode slot mapping; check if #42638 or a follow-up resolves the GPU case.
