# bitwise-34865

- upstream id: 34865
- source type: issue_pr_pair
- mechanism: prefix_cache_equivalence
- current status: stable
- decision: include
- priority: high
- last reviewed: 2026-06-19

## Why not promoted

已 merged；结论绑定 Mamba all-mode、CUDA graph、多个相同 MambaSpec cache group 的 metadata persistent-buffer identity，不外推到所有 Mamba prefix-cache 或 MTP/spec decode 场景。

## Evidence summary

新增 insight：prefix-cache metadata 等价不仅是 block table 值，还包括 CUDA graph replay 读取的 persistent buffer 地址；单 Mamba layer tiny tests 不会触发 metadata cache reuse。

Target evidence: Issue #34865 comments; PR #34874 files: mamba_attn.py update_block_table copies block_idx_last_scheduled_token and block_idx_last_computed_token into current builder persistent buffers; test_mamba_update_block_table.py constructs two builders sharing MambaSpec and asserts storage points to current builder

## Missing evidence

无阻塞性缺口；只保留适用边界和后续回归观察。

## Next action

后续复核 #43650/#43559 时区分 metadata pointer fix 与 MTP/spec decode kernel-selection、cache reassignment 等新问题。
