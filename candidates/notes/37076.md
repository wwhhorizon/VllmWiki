# bitwise-37076

- upstream id: 37076
- source type: issue
- mechanism: kv_cache_identity
- current status: defer_blocked
- decision: defer
- priority: low
- last reviewed: 2026-06-20

## Why not promoted

linked PR #37152 已抓取并直接实现 `_blocks_registered_this_step` miss gate；但 maintainer 在 PR 评论中明确指出 same-step KV sharing 按设计允许，因为写 KV 发生在 attention 之前。closed/unmerged PR #38715 虽把同类 guard 推到 `SingleTypeKVCacheManager` 并补了 `test_no_intra_step_prefix_reuse` 回归测试，但作者在收到该反馈后主动撤回 blanket guard，说明当前缺的已不只是 merge/test，而是前提本身仍被上游质疑。

## Evidence summary

新增 insight：same-step registration visibility / block lifecycle 仍是值得跟踪的调试方向，但“miss gate 本身就是根因修复”并未获得上游接受；`#38715` 更适合作为被回撤的探索线，而不是独立正证。

Target evidence: Issue #37076 body/comments; PR #37152 files/comments; PR #38715 body/comments/files with withdrawn manager-level guard

## Missing evidence

linked PR #37152 已抓取并直接实现 `_blocks_registered_this_step` miss gate；但 maintainer 在 PR 评论中明确指出 same-step KV sharing 按设计允许，因为写 KV 发生在 attention 之前。closed/unmerged PR #38715 虽把同类 guard 推到 `SingleTypeKVCacheManager` 并补了 `test_no_intra_step_prefix_reuse` 回归测试，但作者在收到该反馈后主动撤回 blanket guard，说明当前缺的已不只是 merge/test，而是前提本身仍被上游质疑。

## Next action

按辅助边界维护；仅在出现 runtime instrumentation、lifecycle trace、或 maintainer-accepted patch 明确证明 same-step sharing 设计前提失效时，再升回主线复核。
