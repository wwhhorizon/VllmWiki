# bitwise-27364
- upstream id: 27364
- source type: issue_pr_pair
- mechanism: quant_dtype_semantics
- current status: defer_blocked
- decision: include
- priority: high
- last reviewed: 2026-06-23

## Why defer_blocked
Qwen3-VL FP8 returns only "!!!!!" on Jetson Thor. Directly quant_dtype_semantics: online FP8 quantization path corrupts output for models with biased linears. PR #39666 (closes_keyword) "Add bias to SKIP_TENSORS to fix online FP8 for models with biased linears" — but it fixes #39663 and only says "Likely related to #27364." No direct closure for #27364, no maintainer confirmation that this resolves the Jetson Thor case. Issue still open.

## Evidence summary
Issue open (bug;unstale), 11 comments. PR #39666 (closes_keyword, confidence 0.95) adds bias tensors to SKIP_TENSORS for online FP8. Non-FP8 and --quantize fp8 dynamic paths work fine; only pre-quantized FP8 checkpoint path breaks.

## Next action
Verify if #39666 merge resolves #27364 on Jetson Thor; if confirmed by reporter, promote to stable.
