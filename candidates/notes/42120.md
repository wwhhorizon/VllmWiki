# bitwise-42120
- upstream id: 42120
- source type: pr
- mechanism: quant_dtype_semantics
- current status: stable
- decision: include
- priority: high
- last reviewed: 2026-06-23

## Why stable
PR merged (2026-06-19, ywang96), human approved (jeejeelee). 5 files, +196/-4. Fixes two critical FP8 LoRA MoE bugs: #42007 (quantized hidden states passed to LoRA kernel → silent corruption) and #42008 (no-early-exit for base-model batches → crash on unsupported GPUs). Core mechanism: 3-layer handoff preserves unquantized hidden states for LoRA while base GEMM uses quantized copy. Tested on RTX PRO 6000 Blackwell (SM 12.0). Regrade risk: low (only new code path activates when LoRA+FP8 quant+DP/EP all2all; else modular_kernel stash fallback).

## Evidence summary
Fix: deferred-quantization in triton_moe.py (expects_unquantized_inputs gate), modular_kernel.py stash before _prepare(), punica_gpu.py no_lora_flag early-exit. 3-way lora_x resolution: lora_unquantized_hidden_states → original_hidden_states (row-count guarded) → hidden_states. Regression test: test_add_lora_fused_moe_early_exit.
