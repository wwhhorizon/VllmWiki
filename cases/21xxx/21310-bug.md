# vllm-project/vllm#21310: [Bug]:

| 字段 | 值 |
| --- | --- |
| Issue | [#21310](https://github.com/vllm-project/vllm/issues/21310) |
| 状态 | closed |
| 标签 | bug;stale |
| 评论 | 2; 本地原始数据只有评论数量，没有评论正文 |
| 一级分类 | correctness |
| 工作域 | ci_build;distributed_parallel;frontend_api;hardware_porting;model_support;quantization;sampling_logits |
| 子分类 |  |
| Operator 关键词 | cuda;operator;quantization;triton |
| 症状 | build_error;nondeterministic |
| 根因提示 | dtype;env_dependency;race_condition |
| 硬件范围 | amd;nvidia |
| 需要人工复核 | False |

## 源证据

### Issue 标题

> [Bug]:

### Issue 正文摘录

### Your current environment ### 🐛 Describe the bug When supplying a seed to curl, the model does not produce the same result. Noticed when running with OpenWebUI setting the seed and then repeated with cURL. Supplying the seed to the curl command following [this](https://docs.vllm.ai/en/stable/getting_started/quickstart.html#openai-compatible-server) doesn't seem to appropriately set the seed. Have tested setting a seed with Ollama for the sake of seeing if the model itself can demonstrate reproducible results and it does have exact same results when seed is set. Did find https://github.com/vllm-project/vllm/issues/3333 discussing deterministic results; however, the issue seems to have gone stale. This is a critical issue for scientific implementations because of reproducibility - want/need to be able to share commands that generate identical outputs when publishing. ```bash (base) user$ curl http:// :8000/v1/chat/completions -H "Authorization: Bearer token-abc123" -H "Content-Type: application/json" -d '{"model": "/root/.cache/huggingface/Qwen3-30B-A3B-GPTQ-Int4/", "messages": [{"role": "user", "content": "What is Nearest Neighbor G? Provide a 1 paragraph summary. /no_think"}],...

## 候选优化模式

- [模型格式与 Adapter 路径](../patterns/model_format_adapter.md) - 分数 4: vironment ### 🐛 Describe the bug When supplying a seed to curl, the model does not produce the same result. Noticed when running with OpenWebUI setting the seed and then repeated with cURL. Supplying the seed to the cur...
- [Bitwise 确定性与数值等价](../patterns/bitwise_determinism_equivalence.md) - 分数 3: d with Ollama for the sake of seeing if the model itself can demonstrate reproducible results and it does have exact same results when seed is set. Did find https://github.com/vllm-project/vllm/issues/3333 discussing de...
- [构建、依赖与打包](../patterns/build_dependency_packaging.md) - 分数 3: Ollama for the sake of seeing if the model itself can demonstrate reproducible results and it does have exact same results when seed is set. Did find https://github.com/vllm-project/vllm/issues/3333 discussing determini...
- [Dtype、量化与 Scale 路径](../patterns/dtype_quantization_path.md) - 分数 3: ication/json" -d '{"model": "/root/.cache/huggingface/Qwen3-30B-A3B-GPTQ-Int4/", "messages": [{"role": "user", "content": "What is Nearest Neighbor G? Provide a 1 paragraph summary. /no_think"}], "temperature": 0.5, "ma...
- [硬件架构 Guard](../patterns/hardware_arch_guard.md) - 分数 3: est neighbor, providing insights into the spatial pattern of the data. A smaller average distance suggests clustering, while a larger distance indicates dispersion. This technique is commonly applied in fields such as e...

## Wiki 抽取状态

- 风险：该 issue 有评论，但本地数据只有评论数量，没有评论正文。
- 本地没有 linked-fix 证据；目前只支持症状/路径抽取。
- 后续迭代应在可用时读取完整讨论评论。
