# vllm-project/vllm#40195: [Bug]:

| 字段 | 值 |
| --- | --- |
| Issue | [#40195](https://github.com/vllm-project/vllm/issues/40195) |
| 状态 | open |
| 标签 | bug |
| 评论 | 0; 本地原始数据只有评论数量，没有评论正文 |
| 一级分类 | correctness |
| 工作域 | ci_build;distributed_parallel;frontend_api;gemm_linear;hardware_porting;model_support;quantization;sampling_logits;speculative_decoding |
| 子分类 | precision |
| Operator 关键词 | cuda;operator;sampling;triton |
| 症状 | build_error;nan_inf;slowdown |
| 根因提示 | env_dependency |
| 硬件范围 | amd;nvidia |
| 需要人工复核 | False |

## 源证据

### Issue 标题

> [Bug]:

### Issue 正文摘录

### Your current environment Additional deployment context: - **Model**: GLM-5.1 (tested with `lukealonso/GLM-5.1-NVFP4`) - **Parser**: `--tool-call-parser glm47` (`Glm47MoeModelToolParser`, inheriting `Glm4MoeModelToolParser`) - **Chat template**: GLM-5.1's default tool-call template, which renders string tool-call arguments **bare** (unquoted between ` ` tags) and non-string values via `tojson`. The updated 16.4.2026 version of the template was used (the older had same issue). - **Client**: OpenAI-compatible (any SDK that consumes `stream: true` SSE), I used OpenCode. ### 🐛 Describe the bug ## TL;DR When the `glm47` / `glm4_moe` tool parser streams tool-call arguments, two classes of values get silently corrupted in the `function.arguments` JSON delivered to the client over SSE. Non-streaming (`stream: false`) is unaffected. | Value kind | Expected in arguments | Actual (streaming) | |---|---|---| | `Optional[str]` field (Pydantic `anyOf: [{type: string}, {type: null}]`) | `"Smith"` | `Smithh"` — opening quote missing, last char duplicated | | JSON array / object field | `["Acme","Beta"]` | `["Acme","Beta"]]` — closing bracket duplicated | Regular required `str` fields and `int`...

## 现有链接修复摘要

#42979 [Bugfix] GLM tool parser: fix regex and robustness issues

## 候选优化模式

- [构建、依赖与打包](../patterns/build_dependency_packaging.md) - 分数 6: ween ` ` tags) and non-string values via `tojson`. The updated 16.4.2026 version of the template was used (the older had same issue). - **Client**: OpenAI-compatible (any SDK that consumes `stream: true` SSE), I used Op...
- [验证与 Benchmark](../patterns/verification_benchmarking.md) - 分数 4: environment Additional deployment context: - **Model**: GLM-5.1 (tested with `lukealonso/GLM-5.1-NVFP4`) - **Parser**: `--tool-call-parser glm47` (`Glm47MoeModelToolParser`, inheriting `Glm4MoeModelToolParser`) - **Chat...
- [Dtype、量化与 Scale 路径](../patterns/dtype_quantization_path.md) - 分数 3: oyment context: - **Model**: GLM-5.1 (tested with `lukealonso/GLM-5.1-NVFP4`) - **Parser**: `--tool-call-parser glm47` (`Glm47MoeModelToolParser`, inheriting `Glm4MoeModelToolParser`) - **Chat template**: GLM-5.1's defa...
- [硬件架构 Guard](../patterns/hardware_arch_guard.md) - 分数 3: ional[str]` field (Pydantic `anyOf: [{type: string}, {type: null}]`) | `"Smith"` | `Smithh"` — opening quote missing, last char duplicated | | JSON array / object field | `["Acme","Beta"]` | `["Acme","Beta"]]` — closing...
- [Scheduler 与请求状态生命周期](../patterns/scheduler_request_lifecycle.md) - 分数 3: m.entrypoints.openai.chat_completion.protocol import ( ChatCompletionRequest, ChatCompletionToolsParam, FunctionDefinition, ) from vllm.tokenizers import get_tokenizer from vllm.tool_parsers.glm47_moe_tool_parser import...

## 关联 PR 证据

| PR | 链接类型 | 置信度 | PR 标题 | 证据 |
| --- | --- | ---: | --- | --- |
| [#42979](https://github.com/vllm-project/vllm/pull/42979) | closes_keyword | 0.95 | [Bugfix] GLM tool parser: fix regex and robustness issues | Fixes #40195 |

## Wiki 抽取状态

- 后续迭代应在可用时读取完整讨论评论。
