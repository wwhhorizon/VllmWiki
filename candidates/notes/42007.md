# bitwise-42007

- upstream id: 42007
- source type: issue_pr_pair
- mechanism: quant_dtype_semantics
- current status: stable
- decision: include
- priority: high
- last reviewed: 2026-06-20

## Why not promoted

PR #42120 已于 2026-06-19 合并；主机制闭环为两层：`no_lora_flag_cpu` 早退阻止 stale mapping 污染 base batch，LoRA path 改用 `original_hidden_states` 保证 shrink kernel 看到原始精度 activation 而非量化后的 base activation。wrong input dtype 仍缺专门单测，第三方 Blackwell 验证的 adapter 没有 routed-expert LoRA 权重，因此边界仍需保留，但不再阻止核心机制 promotion。

## Evidence summary

新增 insight：FP8 MoE LoRA 需要同时维护 base GEMM 的量化 activation 与 LoRA delta 的原始精度 activation；无 active LoRA 时必须早退以避免 stale mapping 写坏 base output。`#42120` 合并后，这条机制已可稳定下沉；剩余的是测试覆盖边界，不是根因未闭环。

Target evidence: Issue #42007 comments; PR #42120 files: punica_gpu.py no_lora_flag early return; triton_moe.py expects_unquantized_inputs/original_hidden_states; modular_kernel.py stash and clear original hidden states; review comments on stash timing and cleanup; merged state; Blackwell validation comment

## Missing evidence

无阻塞性缺口；只保留适用边界和后续回归观察。

## Next action

主机制已稳定；后续只补 wrong-input-dtype regression 与 routed-expert LoRA weights 非零的 adapter path 验证。
