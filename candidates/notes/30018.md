# bitwise-30018

- upstream id: 30018
- source type: pr
- mechanism: batch_invariance
- current status: include_with_boundary
- decision: include
- priority: high
- last reviewed: 2026-06-19

## Why not promoted

已 merged，但本地评论明确该 PR 不支持 CUDA graph；early review 曾指出 num_splits 硬编码会破坏 CUDA graph metadata，最终测试依赖 enforce_eager；LoRA 有 PR body、测试矩阵和 review 支持，但 landed-code 子路径仍需补证。

## Evidence summary

新增 insight：split 参数是 dispatch/reduction contract，不是普通调参；FA2 有直接 patch 证据，LoRA 可以作为测试/review-supported boundary，但不能在缺 landed-code 子路径复核时写成完全闭环。

Target evidence: PR #30018 files: flash_attn.py batch-invariant num_splits, batch_invariant.py SM80/SM89 support; PR body/test matrix includes Qwen2.5 LoRA distinct outputs collapse to 1; review says LoRA part LGTM; comments show batch invariant unit tests pass with enforce_eager

## Missing evidence

已 merged，但本地评论明确该 PR 不支持 CUDA graph；early review 曾指出 num_splits 硬编码会破坏 CUDA graph metadata，最终测试依赖 enforce_eager；LoRA 有 PR body、测试矩阵和 review 支持，但 landed-code 子路径仍需补证。

## Next action

寻找后续 FA2 CUDA graph BI support；补 LoRA landed-code split-K 子路径；不要把 #30018 外推为所有 FA2 serving mode 已 deterministic。
