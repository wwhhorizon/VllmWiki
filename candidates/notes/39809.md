# bitwise-39809
- upstream id: 39809
- source type: issue_pr_pair
- mechanism: kv_cache_identity
- current status: include_with_boundary
- decision: include
- priority: high
- last reviewed: 2026-06-23

## Why not promoted
Closed issue with linked bugfix PR #41233, but the case summary describes a chain of three startup crashes across the combined prefix-caching + MTP + hybrid-Mamba path. This is strong evidence for a bounded closure candidate, but I am keeping it at include-with-boundary until the exact changed-files / sub-fix mapping is reviewed more closely.

## Evidence summary
The issue says NemotronH hybrid Mamba2 models crash only when prefix caching (`mamba_cache_mode=all`) is combined with MTP speculative decoding. The linked PR explicitly says it fixes the chain of three bugs behind that startup failure. Mechanistically this fits KV / metadata identity more than generic performance tuning: prefix-cached Mamba state and speculative decoding lifecycle interact incorrectly when multiple cache/state surfaces are composed.

