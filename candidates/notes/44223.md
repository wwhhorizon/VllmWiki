# bitwise-44223
- upstream id: 44223
- source type: issue
- mechanism: prefix_cache_equivalence
- current status: include_with_boundary
- decision: include
- priority: medium
- last reviewed: 2026-06-23

## Why include_with_boundary
RFC for Semantic KV Cache Reuse Interface — proposes engine-side controls for approximate KV cache reuse beyond exact prefix matching. Directly relevant to prefix_cache_equivalence / kv_cache_identity: explicitly defines the boundary between exact prefix-cache semantics (must remain exact) and approximate/semantic reuse (needs explicit cache-commit semantics). Boundary: this is a design RFC with no landed implementation; the insight about exact-vs-approximate cache commit semantics is valuable but not yet a stable mechanism.

## Evidence summary
Issue open (RFC), 1 comment. Proposes Semantic KV Cache Connector Interface. Key principle: "exact prefix-cache writes must remain exact; any approximate reuse needs explicit cache-commit semantics before enabling with prefix caching."

## Next action
Monitor if any PR implements the semantic KV cache connector interface; track whether the exact-vs-approximate boundary crystallizes into a verification contract.
