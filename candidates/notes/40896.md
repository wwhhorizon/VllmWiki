# bitwise-40896

- upstream id: 40896
- source type: issue
- mechanism: prefix_cache_equivalence
- current status: defer_blocked
- decision: defer
- priority: low
- last reviewed: 2026-06-20

## Why not promoted

open issue；H100/Qwen3-0.6B reduced repro 显示 warmed prefix cache 与 no-prefix/cold prefix 输出不同；maintainer 明确说 prefix caching determinism 目前还未完全支持，评论也给出 `VLLM_BATCH_INVARIANT=1` 作为缓解线索。因此当前缺口更接近默认契约边界，而不是单纯等待 root-cause patch/test 闭环。

## Evidence summary

新增 insight：cold/warm prefix cache 差异当前更适合先解释为 batch/query-length 改变 backend geometry 的 exact-reproducibility 边界；`VLLM_BATCH_INVARIANT=1` 是现有上游认可的缓解方向，不是默认路径已修复。

Target evidence: Issue #40896 body/comments: warmed prefix cache 7577->5777; maintainer says prefix caching determinism not fully supported; user comment points to BI mitigation

## Missing evidence

open issue；H100/Qwen3-0.6B reduced repro 显示 warmed prefix cache 与 no-prefix/cold prefix 输出不同；maintainer 明确说 prefix caching determinism 目前还未完全支持，评论也给出 `VLLM_BATCH_INVARIANT=1` 作为缓解线索。因此当前缺口更接近默认契约边界，而不是单纯等待 root-cause patch/test 闭环。

## Next action

按辅助边界维护；继续补 cold/warm/no-prefix/batch-invariant 验证矩阵，并只在出现 prefix-cache 专属 docs/test/patch 时再升回主线 direct-closure 复核。
