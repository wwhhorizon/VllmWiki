# bitwise-43472
- upstream id: 43472
- source type: issue
- mechanism: quant_dtype_semantics
- current status: include_with_boundary
- decision: include
- priority: medium
- last reviewed: 2026-06-23

## Why not promoted
Issue closed as "downstream artifact bug" (canada-quant V4-Pro NVFP4 artifacts perturbed MTP block). Two upstream gaps remain: #43319 merged (.scale suffix fix), per-layer MoE routing in DSV4FP8Config not yet filed. DSV4-Pro specific, MTP path only.

## Evidence summary
Stacked attn FP8 scale loader gap: mtp.py:load_weights renames .scale → .weight_scale_inv via candidate-list logic, then stomps onto fused_wqa_wkv through stacked_params_mapping → KeyError (fused param not registered as per-shard weight_scale_inv). MTP acceptance collapsed to 2.65% vs 91.14% on zyongye fork. Fix: v12 artifact byte-passthrough MTP layer (no transform/transcode/dequant). Mechanism: FP8 scale loading must handle fused stacked params path.
