# bitwise-44395

- upstream id: 44395
- source type: issue
- mechanism: kv_cache_identity
- current status: defer_blocked
- decision: defer
- priority: medium
- last reviewed: 2026-06-22

## Why not promoted

Open issue with clear root-cause direction but no linked fix; partial wake_up(tags=[weights]) leaves KV cache released but engine can still forward, accessing freed KV memory. Only a contributor claim comment, no maintainer response or linked PR.

## Evidence summary

新增索引：KV cache identity 的生命周期维度——partial wake_up 破坏 forward 执行时 KV 必须处于 allocated 状态的隐含前提。无 linked fix，暂不 promotion。

Target evidence: Issue #44395 body: wake_up(tags=[weights]) + forward -> illegal memory access; DP execute_dummy_batch() triggers forward during weights-only window; --enable-sleep-mode + MoE model

## Missing evidence

见上方 Why not promoted；核心缺口已在该段说明。

## Next action

追踪是否出现 linked fix；若有 patch，确认 partial wake_up 后 engine 是否拒绝 forward 或 KV allocation 被 gate。
