# bitwise-42513
- upstream id: 42513
- source type: issue
- mechanism: batch_invariance
- current status: defer_blocked
- decision: defer
- priority: medium
- last reviewed: 2026-06-23

## Why not promoted
Issue is closed, but it was self-closed by the reporter within minutes and has no maintainer response, no linked fix, and no changed files proving closure. The body is still useful as a crisp eager-vs-BI contract example, but it cannot be written as landed behavior.

## Evidence summary
Reporter isolates a deterministic divergence between MTP and non-MTP in eager mode at `temperature=0`, while CUDA graph mode keeps outputs identical. The proposed root-cause chain is batch-size-dependent cuBLAS algorithm selection in eager mode, with BF16 ULP drift amplified through KV cache over many decode steps. The body explicitly says this is distinct from #38938, so the existing EAGLE DP fixes do not close this attention-GEMM contract boundary.
