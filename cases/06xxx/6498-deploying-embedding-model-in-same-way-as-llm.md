# vllm-project/vllm#6498: deploying embedding model in same way as LLM 

| 字段 | 值 |
| --- | --- |
| Issue | [#6498](https://github.com/vllm-project/vllm/issues/6498) |
| 状态 | closed |
| 标签 | feature request;stale |
| 评论 | 5; 本地原始数据只有评论数量，没有评论正文 |

## 源证据

### Issue 标题

> deploying embedding model in same way as LLM 

### Issue 正文摘录

### 🚀 The feature, motivation and pitch can we deploy the embedding model such as bge,e5 etc. using vllm? if yes could u point to the resources for it. ### Alternatives _No response_ ### Additional context _No response_

## 候选优化模式

- [Scheduler 与请求状态生命周期](../patterns/scheduler_request_lifecycle.md) - 分数 2: deploying embedding model in same way as LLM feature request;stale ### 🚀 The feature, motivation and pitch can we deploy the embedding model such as bge,e5 etc. using vllm? if yes could u point to the resources for it....
- [模型格式与 Adapter 路径](../patterns/model_format_adapter.md) - 分数 1: deploying embedding model in same way as LLM feature request;stale ### 🚀 The feature, motivation and pitch can we deploy the embedding model such as bge,e5 etc. using vllm? if yes could u point to the resources for it....

## Wiki 抽取状态

- 风险：该 issue 有评论，但本地数据只有评论数量，没有评论正文。
- 该 issue 不在当前 operator/kernel case 表中；保留索引，但暂不推断优化结论。
- 后续迭代应在可用时读取完整讨论评论。
