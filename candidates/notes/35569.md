# bitwise-35569

- upstream id: 35569
- source type: issue_pr_pair
- mechanism: deterministic_dispatch_reduction
- current status: exclude
- decision: exclude
- priority: N/A
- last reviewed: 2026-06-23

## Why excluded

Issue 本身是 ROCM_ATTN backend 对 Qwen3-VL-Reranker-2B 的 score deviation（~8.5% rel diff），属于 semantic score accuracy 而非 token/logprob/KV identity 或 bitwise 等价。无 root cause、无 fix、无 maintainer response。open/reporter 已标记 "will revisit after version bumps"，但未见进展。PR #39849 是 selector fallback workaround 而非 root-cause fix。

不符合 bitwise 主线准入条件：phenomenon 是 score accuracy（非 token/logprob/tensor identity）。按 agent_loop 规则从 ledger 中移除。

## Evidence summary

ROCM_ATTN backend 在 gfx950 上产生 8.45% score deviation，gfx942 上 3.66%。其他 ROCm backends 保持在 2-3%。No root cause identified。Related: PR #35571, PR #39849 (selector fallback workaround)。

Target evidence: Issue #35569 score drift table; comment from gshtras asking about head size/fallback (unanswered); stale-then-unstale cycle.
