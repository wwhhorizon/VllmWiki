# bitwise-44115

- upstream id: 44115
- source type: pr
- mechanism: deterministic_dispatch_reduction
- current status: defer_blocked
- decision: defer
- priority: medium
- last reviewed: 2026-06-22

## Why not promoted

Open PR (1 comment, 0 review comments, 1 changed file); FlashInfer MoE kernels (TRTLLM/CUTLASS/CuteDSL/b12x) are internal routers that compute top-k inside the kernel, so router capture hook is never invoked and RoutedExpertsCapturer returns all-zero routing. Fix: guard config validation to hard-error on flashinfer+capture, auto+capture falls back to triton.

## Evidence summary

boundary insight: CUDA graph capture incompatible with FlashInfer internal router - top-k computed inside kernel bypassing Python router capture hook; PR still open

Target evidence: PR #44115 snippet: FlashInfer kernels are internal routers, top-k computed inside kernel, select_experts() capture hook never invoked; --moe-backend triton returns real top-8 routing with 128 distinct experts; TRTLLM auto returns all-zero min=0 max=0

## Missing evidence

Open PR (1 comment, 0 review comments, 1 changed file); FlashInfer MoE kernels (TRTLLM/CUTLASS/CuteDSL/b12x) are internal routers that compute top-k inside the kernel, so router capture hook is never invoked and RoutedExpertsCapturer returns all-zero routing. Fix: guard config validation to hard-error on flashinfer+capture, auto+capture falls back to triton.

## Next action

track #44115 merge or RFC #39701 native capture path; if merged verify FlashInfer routed_experts capture returns real top-k
