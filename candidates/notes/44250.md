# bitwise-44250

- upstream id: 44250
- source type: issue
- mechanism: kv_cache_identity
- current status: defer_blocked
- decision: defer
- priority: high
- last reviewed: 2026-06-21

## Why not promoted

linked PR #45549 已抓取，PR body 直接写 Fixes #44250，并把 `lora_name` 折进 LMCache MP internal cache salt；issue comment 还指向 LMCache draft PR #2962。当前两边都仍 open，且截至 2026-06-21 仍无新的 maintainer/reviewer acceptance signal；`#45549` 只证明了 vLLM vendored connector 的 internal salt 层；LMCache 上游 #2962 已经扩到 MP connector、multi-process adapter、chunk-hash / `CacheEngineKey.tags` / L2 object key 等多层 schema，但仍 open/dirty，review 也进一步把争论点收敛到 schema single source of truth：LoRA 维度究竟该统一落在 `CacheEngineKey.tags` 这类主键层，还是继续分散在 request config / token database / cache engine 多处镜像。`lora_name` 也只能证明需要 adapter 维度，不等于最终稳定 version identity。

## Evidence summary

新增 insight：external KV 这条线当前更像“跨仓 schema 仍未统一，且主键落点仍未定案”的系统级缺口，而不是单个 vLLM patch 即将闭环；`#45549` 只是先把 MP connector 的内部 salt 层补上，而 LMCache `#2962` 虽然已经扩到 MP/object-key 多层，但 review 仍在收敛统一 schema 应该由哪一层承载。

Target evidence: Issue #44250 body/comment; PR #45549 files: lmcache_mp_connector.py, test_lmcache_mp_connector.py; LMCache PR #2962 files: lmcache_mp_connector.py, vllm_multi_process_adapter.py, vllm_v1_adapter.py, cache_engine.py, raw_block/key_codec.py, distributed l2 adapters and tests

## Missing evidence

linked PR #45549 已抓取，PR body 直接写 Fixes #44250，并把 `lora_name` 折进 LMCache MP internal cache salt；issue comment 还指向 LMCache draft PR #2962。当前两边都仍 open，且截至 2026-06-21 仍无新的 maintainer/reviewer acceptance signal；`#45549` 只证明了 vLLM vendored connector 的 internal salt 层；LMCache 上游 #2962 已经扩到 MP connector、multi-process adapter、chunk-hash / `CacheEngineKey.tags` / L2 object key 等多层 schema，但仍 open/dirty，review 也进一步把争论点收敛到 schema single source of truth：LoRA 维度究竟该统一落在 `CacheEngineKey.tags` 这类主键层，还是继续分散在 request config / token database / cache engine 多处镜像。`lora_name` 也只能证明需要 adapter 维度，不等于最终稳定 version identity。

## Next action

追踪 #45549 与 LMCache #2962 合并；确认 MP lookup/store key、vendored connector、chunk-hash/CacheEngineKey.tags/L2 object key 是否真正收敛到统一 schema 入口，并保留 same-adapter hit 与跨 adapter negative regression test。
