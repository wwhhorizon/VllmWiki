# bitwise-43355

- upstream id: 43355
- source type: pr
- mechanism: kv_cache_identity
- current status: unresolved_review_risk
- decision: include
- priority: high
- last reviewed: 2026-06-23

## Why not promoted

PR open/unmerged（多次 `needs-rebase`，author 在 2026-05-31/06-12/06-20 force-push 清除后复现）。三处正确的 bot review 缺陷未处理：
1. **CRITICAL**（gemini-code-assist）：`rope_kvcache_fusion_kernels.cu` 中 `raw_kv_scalar_t`（整数类型如 uint16_t）被传给 `fp8::scaled_convert`，导致整数位模式被当作浮点数转换，FP8 output 完全错误。
2. **HIGH**（gemini-code-assist）：`fused_rope_kvcache_supported()` 假设只有 NHD layout，但 `FlashAttentionBackend` 同时支持 NHD/HND，HND 下会导致 silent memory corruption。需添加 `get_kv_cache_layout() == "NHD"` guard。
3. **MEDIUM**（depthfirst-app）：host 函数只校验了 `query.size(0) >= num_tokens`，未校验 key/value，可能导致 OOB GPU memory read/write。

14 位 reviewer requested，无人 approve。

## Evidence summary

添加 native CUDA fused RoPE + KV cache write kernel（9 files, +873/-8）。核心理由：NVIDIA 上 `RopeKVCacheFusionPass` 是一段空代码 —— 无 CUDA 后端实现 `fused_rope_kvcache_supported()`，且 flag gated `is_rocm()`-only。

Kernel design：1 CTA per token，block=min(max(rope_work, cache_work), 512)，Phase1/2 in-place RoPE (fp32 interms)，Phase3 write KV cache。Test：`tests/kernels/attention/test_fused_rope_kvcache.py`（64 passed, bit-identical `rtol=0, atol=0`）—— 但 slot_mapping 采样无重复以规避 "last write wins" non-determinism。

Flag 默认关闭，无用户可见行为变更。

## Missing evidence

见上方 Why not promoted。核心风险：FP8 conversion type bug + HND layout guard + key/value OOB check 均无 follow-up。

## Next action

追踪 follow-up patch 或 maintainer resolution。只有 FP8 conversion 闭环、NHD layout gate 和 key/value size guard 被接受后，才可从 unresolved_review_risk 降级。
