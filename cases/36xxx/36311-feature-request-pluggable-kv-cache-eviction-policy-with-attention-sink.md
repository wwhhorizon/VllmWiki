# vllm-project/vllm#36311: [Feature Request] Pluggable KV cache eviction policy with attention sink protection

| 字段 | 值 |
| --- | --- |
| Issue | [#36311](https://github.com/vllm-project/vllm/issues/36311) |
| 状态 | closed |
| 标签 |  |
| 评论 | 2; 本地原始数据只有评论数量，没有评论正文 |

## 源证据

### Issue 标题

> [Feature Request] Pluggable KV cache eviction policy with attention sink protection

### Issue 正文摘录

--- ## Summary vLLM's block manager evicts KV cache blocks with no awareness of **attention sink tokens** — a small set of tokens (typically 1–2) that absorb 45–55% of total attention mass across nearly all heads and layers. Evicting these tokens silently corrupts generation quality. This issue proposes a pluggable `BlockEvictionPolicy` interface that allows users to protect specific token positions from eviction, with attention sink protection as the reference implementation. --- ## Background: Attention Sinks In pre-norm transformers (Llama, Qwen, Mistral and virtually all modern open LLMs), certain tokens accumulate extremely large hidden-state norms due to residual accumulation. These tokens — called **attention sinks** — attract disproportionate attention mass from nearly every head, regardless of semantic relevance. They act as a pressure release valve: when an attention head has no useful target, it dumps its probability mass here. This phenomenon was formally characterized in: > Sun, S., Canziani, A., & LeCun, Y. (2026). *The Spike, the Sparse and the Sink: Anatomy of Massive Activations and Attention Sinks.* arXiv:2603.05498. ICML 2026. And the catastrophic consequence of...

## 候选优化模式

- [模型格式与 Adapter 路径](../patterns/model_format_adapter.md) - 分数 6: ntation. --- ## Background: Attention Sinks In pre-norm transformers (Llama, Qwen, Mistral and virtually all modern open LLMs), certain tokens accumulate extremely large hidden-state norms due to residual accumulation....
- [Scheduler 与请求状态生命周期](../patterns/scheduler_request_lifecycle.md) - 分数 4: [Feature Request] Pluggable KV cache eviction policy with attention sink protection --- ## Summary vLLM's block manager evicts KV cache blocks with no awareness of **attention sink tokens** — a small set of tokens (typi...
- [验证与 Benchmark](../patterns/verification_benchmarking.md) - 分数 4: ptional, Set from vllm.core.interfaces import BlockEvictionPolicy # Pre-profiled sink positions for common models. # Generated by analyzing attention patterns across 400+ prompts per model. # Source: https://github.com/...
- [构建、依赖与打包](../patterns/build_dependency_packaging.md) - 分数 3: luggable `BlockEvictionPolicy` interface that allows users to protect specific token positions from eviction, with attention sink protection as the reference implementation. --- ## Background: Attention Sinks In pre-nor...
- [KV Cache 容量、压缩与 Offload](../patterns/kv_cache_capacity_offload.md) - 分数 3: [Feature Request] Pluggable KV cache eviction policy with attention sink protection --- ## Summary vLLM's block manager evicts KV cache blocks with no awareness of **attention sink tokens** — a small set of tokens (typi...

## Wiki 抽取状态

- 风险：该 issue 有评论，但本地数据只有评论数量，没有评论正文。
- 该 issue 不在当前 operator/kernel case 表中；保留索引，但暂不推断优化结论。
- 后续迭代应在可用时读取完整讨论评论。
